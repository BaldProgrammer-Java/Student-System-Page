/**
 *  配置请求头类型
 * @type {{headers: {"Content-Type": string}}}
 */
const headersConfig = {
    headers: {
        "Content-Type": "application/x-www-form-urlencoded"
    }
};

/**
 *  创建FormData(Form表单)对象
 * @type {FormData}
 */
const formData = new FormData();

